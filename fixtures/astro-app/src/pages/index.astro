---
import type { CtaProps, HeroProps } from '../lib/blocks';

// Home page (single type) with a storyboard dynamic zone.
export interface CmsContent {
  blocks: (HeroProps | CtaProps)[];
}
---

<h1>Home</h1>
